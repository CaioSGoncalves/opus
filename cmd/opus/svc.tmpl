[Unit]
Description={{.name}}
After=network.target

[Service]
ExecStart={{.bin}}
WorkingDirectory={{.dir}}
Restart=always
RestartSec=5

# Limits
MemoryMax={{.ram}}
CPUQuota={{.cpu}}

[Install]
WantedBy=multi-user.target
